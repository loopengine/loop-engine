# @loop-engine/dsl

## 0.2.0

### Minor Changes

- Add `LoopBuilder` (`packages/dsl/src/builder.ts`) for fluent `LoopDefinition` authoring, with `validateLoopDefinition` on build. Re-export from `@loop-engine/sdk`. Includes publish tarball validation and CI checks.
